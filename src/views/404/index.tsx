import React, {useState} from "react";
import {NotFoundStyles as Styled} from "./styles"

export function NotFound() {
    return (
        <Styled.MainContainer>
            <Styled.ContainerPic/>
        </Styled.MainContainer>
    );
}
